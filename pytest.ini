[pytest]
# The real test suite lives entirely under tests/. Scope discovery there so
# pipeline/test_orchestration.py -- a manual, script-style harness whose
# test_stage_*(test_dir) functions take a positional arg pytest can't supply
# (they aren't fixtures) -- is not collected and does not error at collection.
testpaths = tests
