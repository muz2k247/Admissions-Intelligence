# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Never commit real env values (Firebase config is not secret, but the file
# pattern is excluded regardless per CLAUDE.md's never-commit-.env rule).
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
