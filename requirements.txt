requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
cryptography>=42.0
certifi>=2024.2.2
pyyaml>=6.0
pdfplumber>=0.11
playwright>=1.45
pytest>=8.0

# dev-only: generates a real PDF fixture for tests/test_scraper.py's
# happy-path PDF-extraction test — not needed to run the pipeline itself.
reportlab>=4.0
